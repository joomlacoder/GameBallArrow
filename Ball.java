class Ball {
    int x, y, d, speed, direction = 3;

    Ball(int x, int y, int d, int direction) {
        this.x = x;
        this.y = y;
        this.d = d;
        this.direction = direction;
    }
}